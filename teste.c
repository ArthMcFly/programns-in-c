#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
	int main(){
		char letter[25];
		int le;
		scanf("%s", letter);
		le = strlen(letter);
		printf("%d\n", le);
		return 0;
	}
